---
name: Config Sync

on:
  schedule:
    - cron: "0 0 * * 1"
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: write

jobs:
  sync:
    uses: DevSecNinja/.github/.github/workflows/config-sync.yml@195660c2cde322b4f9402a20be4b2dac2fef77e0 # main
    permissions:
      contents: write
      pull-requests: write
    # Uncomment to also bootstrap template files for new repos:
    # with:
    #   sync-templates: true
