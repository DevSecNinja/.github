---
name: Labeler

on:
  workflow_dispatch:
  pull_request:
    branches:
      - $default-branch

permissions:
  contents: read

jobs:
  labeler:
    uses: DevSecNinja/.github/.github/workflows/labeler.yml@main
    permissions:
      contents: read
      pull-requests: write
