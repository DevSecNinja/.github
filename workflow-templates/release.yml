---
name: Release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: read

jobs:
  release:
    uses: DevSecNinja/.github/.github/workflows/release.yml@533c9fa80c07dc081dbd86d48efb97cd479828c9 # v1.0.0
    permissions:
      contents: write
    with:
      # renovate: datasource=github-releases depName=jdx/mise
      mise-version: "2026.4.9"
      tag: ${{ github.ref_name }}
