---
name: Assign issue to CODEOWNERS

on:
  workflow_dispatch: # checkov:skip=CKV_GHA_7: Not a build workflow — input is an issue number for manual assignment
    inputs:
      issue-number:
        description: "Issue number to assign (leave empty for all open issues)"
        required: false
        type: number
  issues:
    types:
      - opened

permissions:
  contents: read

jobs:
  assign:
    uses: DevSecNinja/.github/.github/workflows/assign-issue-to-codeowners.yml@d5381d4da98c75b3c86c6b4699616d176090824f # v1.2.0
    with:
      issue-number: ${{ inputs.issue-number || 0 }}
    permissions:
      contents: read
      issues: write
